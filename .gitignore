# All files tracked — including binaries

# Claude Code local session settings (machine-specific, not for repo)
.claude/

# graphify pipeline temp files
.graphify_*.json
.graphify_*.txt

# Python bytecode cache (build scripts under scripts/)
__pycache__/
*.pyc
