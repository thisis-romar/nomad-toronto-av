# Claude Code local session settings (machine-specific, not for repo)
.claude/

# graphify pipeline temp files
.graphify_*.json
.graphify_*.txt

# --- Monorepo / Node ---
node_modules/

# Build outputs (regenerated by `turbo run build`)
dist/
.astro/

# Turborepo local cache
.turbo/

# --- Cloudflare Pages / local secrets (NEVER commit) ---
# Local dev secrets for the portal: shared password, session secret, allowlist.
# See apps/portal/.dev.vars.example for the committed template.
.dev.vars
.wrangler/

# Environment files
.env
.env.local
