name: Sync issues to project

on:
  issues:
    types: [opened, reopened]
  pull_request:
    types: [opened, reopened]

jobs:
  add-to-project:
    name: Add issue/PR to Nomad Toronto AV project
    runs-on: ubuntu-latest
    steps:
      - uses: actions/add-to-project@v1.0.2
        with:
          project-url: https://github.com/users/thisis-romar/projects/5
          github-token: ${{ secrets.PROJECT_PAT }}
